# Optional: Add your OpenAI API key below to use GPT classification
# If not set, the system will use the built-in keyword-based AI classifier (no cost, no API needed)

AZURE_OPENAI_KEY=""
AZURE_OPENAI_ENDPOINT="https://<your-resource>.openai.azure.com/"
DEPLOYMENT_NAME="<your-deployment-name>"
